# Email Network Motif Analysis

This project discovers and visualizes recurring communication patterns (motifs) in an email network.

## Features

- Extracts **subgraphs/motifs** from large networks using graph pattern mining.  
- Supports **anchor nodes** to track motif growth origins.  
- **Visualizes motifs** with node labels, anchor nodes (red squares), and edge types.  
